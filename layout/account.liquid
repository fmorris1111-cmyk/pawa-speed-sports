<!doctype html>
<html data-wf-page="6a6112d56372627064b3ade9" data-wf-site="6a6112d56372627064b3ae10" lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <title>{{ page_title }}</title>
  <meta content="{{ page_description }}" name="description">
  <meta content="{{ page_title }}" property="og:title">
  <meta content="{{ page_description }}" property="og:description">
  <meta content="{{ page_title }}" name="twitter:title">
  <meta content="{{ page_description }}" name="twitter:description">
  <meta property="og:type" content="website">
  <meta content="summary_large_image" name="twitter:card">
  <meta content="width=device-width, initial-scale=1" name="viewport">
  <meta content="Webflow" name="generator">
  <link href="{{ 'normalize.css' | asset_url }}" rel="stylesheet" type="text/css">
  <link href="{{ 'webflow.css' | asset_url }}" rel="stylesheet" type="text/css">
  <link href="{{ 'webflow-template-42a421-7c8accd486156d1.webflow.css' | asset_url }}" rel="stylesheet" type="text/css">
  <link href="https://fonts.googleapis.com" rel="preconnect">
  <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous">
  <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
  <script type="text/javascript">WebFont.load({  google: {    families: ["Open Sans:300,300italic,400,400italic,600,600italic,700,700italic,800,800italic","Roboto Mono:100,200,300,400,500,600,700"]  }});</script>
  <script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script>
  
            {% if settings.favicon == blank %}
              <link href="{{ 'favicon.png' | asset_url }}" rel="shortcut icon" type="image/x-icon">
            {% else %}
              <link rel="shortcut icon" type="image/png" href="{{ settings.favicon | img_url: '32x32' }}">
            {% endif %}
        
  
            {% if settings.apple_touch_icon == blank %}
              <link href="{{ 'webclip.png' | asset_url }}" rel="apple-touch-icon">
            {% else %}
              <link rel="apple-touch-icon" type="image/png" href="{{ settings.apple_touch_icon | img_url: '32x32' }}">
            {% endif %}
        <!--  Alpine Focus Plugin  -->
  <script defer="" src="https://cdn.jsdelivr.net/npm/@alpinejs/focus@3.x.x/dist/cdn.min.js"></script>
  <!--  Alpine JS  -->
  <script defer="" src="{{ 'alpinejs3.js' | asset_url }}"></script>
  <!--  Liquify Pro Debugger  -->
  <script defer="" auto="" min="" src="https://cdn.jsdelivr.net/gh/liquify-pro/core@latest/script.js"></script>
  <!--  &#9472;&#9472; PhotoSwipe v5 &#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;  -->
  <link rel="stylesheet" href="https://unpkg.com/photoswipe@5/dist/photoswipe.css">
  <!--  &#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;  -->
  <link href="{{ canonical_url }}" rel="canonical">
  {% if page_image %}
  <meta property="og:image" content="http:{{ page_image | image_url }}">
  <meta property="og:image:secure_url" content="https:{{ page_image | image_url }}">
  <meta property="og:image:width" content="{{ page_image.width }}">
  <meta property="og:image:height" content="{{ page_image.height }}">
{% endif %}
  {% case template.name %}
  {% when 'product' %}
<script type="application/ld+json">
  {{ product | structured_data }}
</script>
{% when 'article' %}
<script type="application/ld+json">
  {{ article | structured_data }}
</script>
{% when 'collection' %}
<script type="application/ld+json">
                  {
                     "@context":"http:\/\/schema.org",
                     "@type":"WebPage",
                     "url":"{{ shop.url }}",
                     "mainEntity":{"@type":"offerCatalog",
                     "name": "{{ collection.title }}",
                     "url": "{{ collection.url }}",
                     "numberOfItems":{{ collection.products.size }}}
              }
</script>
{% endcase %}
{% assign min_price = null %}
{% assign max_price = 0 %}
{% for product in collections.all-products.products %}
  {% for variant in product.variants %}
    {% assign price = variant.price | money_without_currency | replace: ',', '.' | plus: 0 %}
    {% if min_price == null or price < min_price %}
      {% assign min_price = price %}
    {% endif %}
    {% if price > max_price %}
      {% assign max_price = price %}
    {% endif %}
  {% endfor %}
{% endfor %}
    <script type="application/ld+json">
      {
        "@context" : "https://schema.org",
        "@type" : "LocalBusiness",
        "mainEntityOfPage" : {
          "@type" : "WebPage",
          "@id" : "{{ shop.url }}"
        },
        "name" : "{{ shop.name }}",
        "telephone" : "{{ shop.phone }}",
        "email" : "{{ shop.email }}",
        "priceRange" : "{{ min_price | money }} - {{ max_price | money }}",
        "address" : {
          "@type" : "PostalAddress",
          "streetAddress" : "{{ shop.address.address1 }}",
          "addressLocality" : "{{ shop.address.city }}",
          "addressRegion" : "{{ shop.address.province }}",
          "addressCountry" : "{{ shop.address.country_code }}",
          "postalCode" : "{{ shop.address.zip }}"
        },
        "url" : "{{ shop.url }}"
      }
    </script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  <style>
  [x-cloak] { display: none !important; }
  [li-cloak_disabled] { display: none !important; }
  ::selection {
    color: #FFFFFF;
    background: #000000;
  }
  ::-moz-selection {
    color: #FFFFFF;
    background: #000000;
  }
  .swiper-slide.is-active .product-header_thumbnail-image {
    opacity: 1;
  }
</style><!--  
IMPORTANT: If you want to make changes to the head or body code on this page, 
please remove the `li-layout` tag from the body element. 
This will create a separate layout file for this page, allowing you to apply custom modifications.
Learn more: https://www.liquify.pro/docu/getting-started#template-structure
 -->
<script src="{{ 'li_helper.js' | asset_url }}" type="text/javascript"></script>{{ content_for_header }}</head>
<body li-page="account">
  <div class="page-wrapper">
    {% render 'global_styles' %}
    {% sections 'header_group' %}
    {{ content_for_layout }}
    {% sections 'footer_group' %}
  </div>
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=6a6112d56372627064b3ae10" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'webflow.js' | asset_url }}" type="text/javascript"></script>
  {% render 'search_javascript' %}
  <!--  &#9472;&#9472; PhotoSwipe v5 + Swiper &mdash; Product Gallery Zoom &#9472;&#9472;  -->
  <script type="module">
  if (!document.querySelector(".swiper-wrapper.is-product-gallery")) {
    throw new Error("PhotoSwipe init skipped: no product gallery on this page.");
  }
  const { default: PhotoSwipeLightbox } = await import("https://unpkg.com/photoswipe@5/dist/photoswipe-lightbox.esm.js");
  const { default: PhotoSwipe }         = await import("https://unpkg.com/photoswipe@5/dist/photoswipe.esm.js");
  await new Promise((resolve) => requestAnimationFrame(resolve));
  document.querySelectorAll(".swiper-container.is-product-gallery").forEach((container) => {
    initGalleryLightbox(container);
  });
  function initGalleryLightbox(container) {
    const allSlides = Array.from(container.querySelectorAll(".swiper-slide.is-product-gallery"));
    const photoswipeItems = [], pswpIndexMap = [], swiperToPswpMap = {};
    allSlides.forEach((slide, swiperIndex) => {
      const img = slide.querySelector("img.product-header_gallery-image");
      if (!img) return;
      const src = img.dataset.zoomSrc || img.src;
      const width = img.naturalWidth || img.width || 0;
      const height = img.naturalHeight || img.height || 0;
      const pswpIndex = photoswipeItems.length;
      photoswipeItems.push({ src, width, height, swiperIndex });
      pswpIndexMap.push(swiperIndex);
      swiperToPswpMap[swiperIndex] = pswpIndex;
    });
    if (photoswipeItems.length === 0) return;
    const galleryEl = container.querySelector(".swiper.is-product-gallery");
    const getSwiperInstance = () => galleryEl && galleryEl.swiper;
    allSlides.forEach((slide, swiperIndex) => {
      const img = slide.querySelector("img.product-header_gallery-image");
      if (!img) return;
      img.style.cursor = "zoom-in";
      img.addEventListener("click", () => openLightbox(swiperIndex));
    });
    const lightbox = new PhotoSwipeLightbox({
      dataSource: photoswipeItems, pswpModule: PhotoSwipe,
      initialZoomLevel: "fit", secondaryZoomLevel: 2, maxZoomLevel: 4,
      wheelToZoom: true, padding: { top: 20, bottom: 20, left: 20, right: 20 },
      loop: false, showHideAnimationType: "fade",
    });
    lightbox.on("loadComplete", (e) => {
      const { slide } = e;
      if (slide.data.width === 0 || slide.data.height === 0) {
        const img = slide.content.element;
        if (img && img.naturalWidth) {
          slide.data.width = img.naturalWidth;
          slide.data.height = img.naturalHeight;
          slide.updateContentSize();
        }
      }
    });
    lightbox.on("change", () => {
      const pswp = lightbox.pswp;
      if (!pswp) return;
      const swiperIndex = pswpIndexMap[pswp.currIndex];
      if (swiperIndex == null) return;
      const swiper = getSwiperInstance();
      if (swiper && swiper.activeIndex !== swiperIndex) swiper.slideTo(swiperIndex, 0);
    });
    lightbox.init();
    function openLightbox(swiperIndex) {
      const pswpIndex = swiperToPswpMap[swiperIndex];
      if (pswpIndex == null) return;
      lightbox.loadAndOpen(pswpIndex);
    }
  }
</script>
  <!--  &#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;&#9472;  -->
  <script>
  document.querySelectorAll('.announcement-bar_container').forEach(function (el) {
    el.setAttribute('data-nosnippet', '');
  });
</script>
  <script>
document.addEventListener('DOMContentLoaded', function () {
  /* &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
     SHOP SUBMENU
  &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552; */
  var leftPill = document.querySelector('.nav_menu-2');
  var drawer   = document.querySelector('.nav_submenu-drawer');
  var panels   = document.querySelectorAll('.nav_submenu-panel');
  var triggers = document.querySelectorAll('.nav_link-item[data-trigger]');
  if (drawer) {
    var subCloseTimer = null;
    gsap.set(drawer, { height: 0, overflow: 'hidden' });
    gsap.set(panels, { display: 'none' });
    function openPanel(key) {
      clearTimeout(subCloseTimer);
      var panel = drawer.querySelector('[data-panel="' + key + '"]');
      if (!panel || !panel.querySelector('a')) { closeDrawer(); return; }
      gsap.set(panels, { display: 'none' });
      gsap.set(panel, { display: 'block' });
      gsap.killTweensOf(drawer);
      if (leftPill) leftPill.classList.add('submenu-is-open');
      gsap.to(drawer, { height: panel.scrollHeight, duration: 0.45, ease: 'power2.out' });
    }
    function closeDrawer() {
      clearTimeout(subCloseTimer);
      if (leftPill) leftPill.classList.remove('submenu-is-open');
      gsap.killTweensOf(drawer);
      gsap.to(drawer, {
        height: 0, duration: 0.35, ease: 'power2.inOut',
        onComplete: function () { gsap.set(panels, { display: 'none' }); }
      });
    }
    function scheduleSubClose() {
      subCloseTimer = setTimeout(closeDrawer, 250);
    }
    triggers.forEach(function (t) {
      t.addEventListener('mouseenter', function () { openPanel(this.dataset.trigger); });
      t.addEventListener('mouseleave', scheduleSubClose);
    });
    drawer.addEventListener('mouseenter', function () { clearTimeout(subCloseTimer); });
    drawer.addEventListener('mouseleave', scheduleSubClose);
    document.addEventListener('click', function (e) {
      if (!e.target.closest('.nav_menu-2')) closeDrawer();
    });
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') closeDrawer();
    });
  }
  /* &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
     MINI CART
  &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552; */
  var cartPill   = document.querySelector('.nav_menu-3');
  var cartHolder = document.querySelector('.nav_mini-cart-holder');
  var cartToggle = document.querySelector('.nav_dropdown.is-cart .w-dropdown-toggle');
  if (cartHolder && cartToggle) {
    gsap.set(cartHolder, { height: 0, overflow: 'hidden' });
    var cartCloseTimer = null;
    var autoCloseTimer = null;
    var isHovering     = false;
    cartToggle.setAttribute('data-minicart-trigger', '');
    function openCart() {
      clearTimeout(cartCloseTimer);
      clearTimeout(autoCloseTimer);
      if (cartPill) cartPill.classList.add('cart-is-open');
      gsap.killTweensOf(cartHolder);
      gsap.to(cartHolder, { height: 'auto', duration: 0.5, ease: 'power2.out' });
    }
    function closeCart() {
      clearTimeout(cartCloseTimer);
      clearTimeout(autoCloseTimer);
      if (cartPill) cartPill.classList.remove('cart-is-open');
      gsap.killTweensOf(cartHolder);
      gsap.to(cartHolder, { height: 0, duration: 0.35, ease: 'power2.inOut' });
    }
    function scheduleCartClose() {
      cartCloseTimer = setTimeout(closeCart, 250);
    }
    cartToggle.addEventListener('mouseenter', function () { isHovering = true;  openCart(); });
    cartToggle.addEventListener('mouseleave', function () { isHovering = false; scheduleCartClose(); });
    cartHolder.addEventListener('mouseenter', function () { isHovering = true;  clearTimeout(cartCloseTimer); });
    cartHolder.addEventListener('mouseleave', function () { isHovering = false; scheduleCartClose(); });
    var _fetch = window.fetch;
    window.fetch = function () {
      var args = arguments;
      var url  = typeof args[0] === 'string' ? args[0] : (args[0] && args[0].url) || '';
      var p    = _fetch.apply(this, args);
      if (url.includes('/cart/add')) {
        p.then(function (r) {
          if (r.ok) {
            openCart();
            clearTimeout(autoCloseTimer);
            autoCloseTimer = setTimeout(function () {
              if (!isHovering) closeCart();
            }, 3000);
          }
        });
      }
      return p;
    };
  }
});
</script>
</body>
</html>